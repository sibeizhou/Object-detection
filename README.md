# Object-detection
Implemented a simplified SSD detector with multi-scale prediction branches at 10x10, 5x5, 3x3, and 1x1 resolutions, generating 540 default boxes per image. o Built anchor generation, IoU-based ground-truth matching, bounding-box regression targets, and multi-class prediction outputs for object detection training.
